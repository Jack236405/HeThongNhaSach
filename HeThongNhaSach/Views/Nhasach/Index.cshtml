@model IEnumerable<HeThongNhaSach.Models.Nhasach>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenns)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diachi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Std)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenns)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diachi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Std)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mans">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Mans">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Mans">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
